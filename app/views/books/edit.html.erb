<% content_for :title, "Editing book" %>

<h1><%= t(".title") %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t("books.actions.show"), @book %> |
  <%= link_to t("books.actions.back"), books_path %>
</div>
