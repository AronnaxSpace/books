<%= render @book, show_details: true %>

<div>
  <%= link_to t(".edit"), edit_book_path(@book) %> |
  <%= link_to t("books.actions.back"), books_path %>

  <%= button_to t(".destroy"), @book, method: :delete, class: "btn btn-danger" %>
</div>
